import 'package:flutter/material.dart';

class MostPopularProductListView extends StatelessWidget {
  const MostPopularProductListView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
